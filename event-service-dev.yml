server:
  port: 8083
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/event_db
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
venue-service:
  name: VENUE-SERVICE
